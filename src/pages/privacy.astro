---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Privacy Policy - Side Badger">
  <section class="legal-page">
    <div class="container">
      <div class="legal-header fade-up">
        <h1>Privacy Policy</h1>
        <p class="text-muted">Last updated: {new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</p>
      </div>

      <div class="legal-content glass fade-up">
        <div class="placeholder-notice">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"/>
            <line x1="12" y1="16" x2="12" y2="12"/>
            <line x1="12" y1="8" x2="12.01" y2="8"/>
          </svg>
          <h2>Privacy Policy Coming Soon</h2>
          <p>The actual privacy policy content will be loaded from the Side Badger API.</p>
          <p class="text-muted">In the meantime, here's what you need to know:</p>
        </div>

        <div class="quick-summary">
          <h3>TL;DR - The Badger's Promise</h3>
          <ul>
            <li><strong>We don't track you.</strong> No analytics, no pixels, no creepy following you around the internet.</li>
            <li><strong>We don't sell your data.</strong> We don't even collect data worth selling.</li>
            <li><strong>We don't show ads.</strong> So we don't need to profile you for ad targeting.</li>
            <li><strong>Your expenses are yours.</strong> We're not mining your data for "insights."</li>
            <li><strong>Minimal data collection.</strong> Just what's needed to make the app work.</li>
          </ul>
        </div>

        <div class="api-placeholder">
          <code>// Privacy policy content will be fetched from:</code>
          <code>// GET app.sidebadger.com/api/legal/privacy</code>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .legal-page {
    padding: var(--space-4xl) 0;
    min-height: 70vh;
  }

  .legal-header {
    text-align: center;
    margin-bottom: var(--space-2xl);
  }

  .legal-header h1 {
    margin-bottom: var(--space-sm);
  }

  .legal-content {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--space-2xl);
  }

  .placeholder-notice {
    text-align: center;
    padding: var(--space-2xl);
    margin-bottom: var(--space-xl);
  }

  .placeholder-notice svg {
    color: var(--accent-secondary);
    margin-bottom: var(--space-lg);
  }

  .placeholder-notice h2 {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-md);
  }

  .placeholder-notice p {
    max-width: none;
    margin-bottom: var(--space-sm);
  }

  .quick-summary {
    background: rgba(52, 211, 153, 0.1);
    border: 1px solid rgba(52, 211, 153, 0.3);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
    margin-bottom: var(--space-xl);
  }

  .quick-summary h3 {
    color: var(--accent-success);
    font-size: var(--text-lg);
    margin-bottom: var(--space-md);
  }

  .quick-summary ul {
    list-style: none;
  }

  .quick-summary li {
    padding: var(--space-sm) 0;
    padding-left: var(--space-lg);
    position: relative;
  }

  .quick-summary li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--accent-success);
  }

  .api-placeholder {
    background: var(--bg-deep);
    border-radius: var(--radius-md);
    padding: var(--space-lg);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--text-muted);
  }

  .api-placeholder code {
    display: block;
    margin-bottom: var(--space-xs);
  }
</style>
